# 1280×720 布局验收记录

> Wave D · T10-UI-10 · 2026-06-10

## 环境

- 分辨率：1280×720（窗口化）
- 主题：深色 studio
- 版本：v10 Wave D

## 检查项

| 区域 | 结果 | 说明 |
|------|------|------|
| TopBar | ✅ | 项目名截断正常；模式切换居中 |
| Dock + 抽屉 | ✅ | 320px 抽屉不遮挡画布主操作区 |
| 画布 | ✅ | ReactFlow 控件可见；Slash _palette 不溢出 |
| Inspector (<1280) | ✅ | 窄屏浮层 + 遮罩，不与 Dock 重叠 |
| Generator Drawer | ✅ | 收起后 TopBar 显示生成进度 Badge |
| 开项目 + 选中节点生成 | ✅ | 可完成图片节点打开 Drawer 并触发生成（mock/真实 Key） |

## 已知限制

- 工作台合成全屏模式下 Dock 隐藏，属预期。
- Agent 钉选时 Inspector 让位，1280 宽下画布区域略窄但可用。

## 结论

1280×720 下可完成「开项目 → 画布编辑 → 生成 → 保存」主路径，无 blocking 重叠。
