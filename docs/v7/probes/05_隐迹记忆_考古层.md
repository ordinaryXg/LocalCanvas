# 探针 05 — 隐迹记忆：考古层

> **实验 E6**：召回后 7 日内再次召回率 >40%。  
> **依赖**：探针 03（archive）；探针 02（临时行星）；探针 06（怀旧者加权）。

---

## 0. 落地原型边界

### Phase 0（3 天）

- 拒绝自动记入 + 地层列表 + 规则检索召回
- metaphor-only 注入模板

### Phase 1（+2 天）

- 剖面可视化、层合并、自然语言 recall（LLM 解析 query）

---

## 1. 用户旅程

| 步 | 用户 | 系统 |
|----|------|------|
| 1 | 扇形按 X 删候选 | toast `记入地层？[原因]` 3s 默认记入 |
| 2 | 侧栏打开「地层」 | 按 depth DESC 列表 |
| 3 | 输入召回「忧郁」 | tags/emotion 检索 Top-3 |
| 4 | 预览注入文案 | 确认 → 临时共振源 gravity=0.3 |
| 5 | 生成 | prompt 仅含 metaphor，无服装细节 |

---

## 2. 界面设计规范

### 2.2 地层侧栏 `PalimpsestStrataPanel`

| 区域 | 规格 |
|------|------|
| 宽 | 280px，与 ToolPanel 同级 Tab |
| 剖面图 | 左 48px 窄条，层色 `depth % 7` 色轮 |
| 层卡片 | 高 72px，缩略 40px + 标签 + tags chips |
| 操作 | 静音🔇 / 复活↑ / 展开详情 |

### 2.2 记入 Toast `PalimpsestCaptureToast`

| 属性 | 值 |
|------|-----|
| 位置 | 底中，上 Compose 栏 |
| 宽 | 360px，高 auto |
| 倒计时 | 3s 环形进度 |
| 输入 | 可选 `原因` 单行 |

### 2.3 召回面板

| 元素 | 说明 |
|------|------|
| 搜索框 | placeholder `忧郁 / 第7层 / 红色` |
| 结果 | 3 卡，显示 `将注入：…` |
| 确认钮 | `复活到共鸣场` |

### 2.4 层详情 Drawer

- 原图/文案、`metaphorTags`、`emotionalSignature` 雷达图（简版两轴）

---

## 3. 状态机

```
reject event --> capturing (3s) --> layer_created
layer: active --mute--> muted (vitality=0)
layer: active --revive--> boosted (vitality=1, temp resonance)
recall query --> ranked --> preview --> applied | cancelled
```

---

## 4. 数据模型

```typescript
interface PalimpsestLayer {
  id: string
  projectId: string
  depth: number
  eventType: 'reject' | 'abandon' | 'brief' | 'manual_note'
  assetPath?: string
  textSnapshot?: string
  userReason?: string
  metaphorTags: string[]
  emotionalSignature: { arousal: number; valence: number }
  vitality: number
  mergedIntoId?: string
  createdAt: string
}
```

```sql
CREATE TABLE palimpsest_layers (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL,
  depth INTEGER NOT NULL,
  event_type TEXT NOT NULL,
  payload TEXT NOT NULL,
  metaphor_tags TEXT NOT NULL DEFAULT '[]',
  emotional_signature TEXT NOT NULL,
  vitality REAL NOT NULL DEFAULT 1,
  merged_into_id TEXT,
  created_at TEXT NOT NULL
);
CREATE UNIQUE INDEX idx_palimpsest_depth ON palimpsest_layers(project_id, depth);
```

### 检索评分

```typescript
function scoreLayer(query: RecallQuery, layer: PalimpsestLayer): number {
  if (layer.vitality <= 0) return -1
  let s = 0
  if (query.tags) s += jaccard(query.tags, layer.metaphorTags) * 0.5
  if (query.emotion) {
    const d = dist(query.emotion, layer.emotionalSignature)
    s += (1 - d / 2) * 0.4
  }
  if (query.layerHint != null && layer.depth === query.layerHint) s += 0.3
  if (chorusNostalgicWeight > 0.6 && layer.eventType === 'brief') s += 0.15
  return s
}
```

### 注入模板（硬编码）

```typescript
export const METAPHOR_INJECT_PREFIX =
  'Apply mood and metaphor only, do NOT copy exact faces, costumes, or layouts: '
```

---

## 5. 实现路线图

### Day 1

- [ ] `palimpsest-repository.ts`
- [ ] `appendLayer` on superposed archive hook
- [ ] `extract-layer.ts` LLM（异步，先占位 tags）

### Day 2

- [ ] `PalimpsestStrataPanel` 列表
- [ ] `PalimpsestCaptureToast`
- [ ] vitality 静音/复活

### Day 3

- [ ] `recall()` + preview modal
- [ ] 应用 → `resonance:create` 临时 phrase 源

### Day 4–5

- [ ] 剖面条、层合并 Jaccard>0.8
- [ ] NL query → LLM 解析为 `RecallQuery`

---

## 6. 代码挂接清单

| 钩子位置 | 动作 |
|----------|------|
| `superposed:archive` 成功回调 | `palimpsest:appendLayer` |
| `HistoryPanel` 删除/拒绝 | 同上（扩展 history IPC） |
| `ResonancePanel` | 「从地层召回」入口 |

```
electron/utility/services/palimpsest/extract-layer.ts
electron/utility/services/palimpsest/recall.ts
electron/main/repositories/palimpsest-repository.ts
electron/main/ipc/palimpsest.ts
src/components/palimpsest/PalimpsestStrataPanel.tsx
src/components/palimpsest/PalimpsestCaptureToast.tsx
src/components/palimpsest/RecallPreviewModal.tsx
```

### LLM Extract Prompt（节选）

```
你是创作考古学家。根据被拒绝的素材，输出 JSON：
metaphorTags: 3-5 个抽象隐喻词，禁止具体人脸服饰品牌
emotionalSignature: { arousal, valence }
禁止描述可复现的视觉细节。
```

---

## 7. 测试用例与验收

| ID | 用例 | 通过标准 |
|----|------|----------|
| T05-1 | append 后 depth 递增 | 无重复 depth |
| T05-2 | recall「melancholy」 | 含 tag 或低 valence 层 |
| T05-3 | vitality=0 不参与 | score=-1 |
| M05-1 | 删除候选 | 3s 内地层+1 |
| M05-2 | 召回确认 | 共鸣盘多一颗低引力行星 |
| E6 | 7日再次召回率 | >40% |

**隐私**：设置 → 清空地层；`project:export` 可选排除表。

---

## 8. 风险

| 风险 | 对策 |
|------|------|
| LLM 泄露具体脸 | 后处理过滤名词 NER 可选 |
| 层爆炸 | 合并相似层；上限 200/项目 |
