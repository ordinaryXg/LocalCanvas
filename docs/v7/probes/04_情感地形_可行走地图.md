# 探针 04 — 情感地形：可行走地图

> **实验 E3**：用地形规划的用户，「分镜→首帧」路径少 2 步。  
> **依赖**：探针 12（compile affect → storyboard）；探针 08（画笔停顿触发预演）。

---

## 0. 落地原型边界

### Phase 0（3 天）

- 2D 曲线编辑器（arousal）+ valence 填色
- 悬崖检测 + toast 建议
- `affectAtTime` 注入生成

### Phase 1（+3 天）

- 锚点、行走预览、分镜格时长联动
- `terrain_suggestions` 持久化

---

## 1. 用户旅程

| 步 | 用户 | 系统 |
|----|------|------|
| 1 | 新建项目设时长 30s | `affect:init(30)` |
| 2 | 画笔刷出前 10s 低 arousal | 更新 `arousalSeries` |
| 3 | 10–12s 刷垂直崖 | `detectCliffs` → 建议条 |
| 4 | 点「插入黑场」 | compileDown 插入 1.2s gap |
| 5 | 分镜格生成 | prompt 含 `arousal=0.32 valence=-0.4` |

---

## 2. 界面设计规范

### 2.1 编辑器 `AffectTerrainEditor`

| 属性 | 值 |
|------|-----|
| 画布 | 主区域宽 100%，高 240px（可拉高到 320） |
| 内边距 | top 16, right 48（Y轴标签）, bottom 32, left 48 |
| X 轴 | 时间刻度每 5s |
| Y 轴 | arousal 0–1，标签 Calm / Intense |
| 曲线 | `stroke-violet-400` 2px，下填充 gradient |
| valence 填色 | `arousalSeries` 下：`hsl(220, 60%, 40% + valence*20%)` |

### 2.2 画笔

| 模式 | 光标 | 行为 |
|------|------|------|
| 抬高 | 圆 brush r=12 | 增加 arousal，高斯笔锋 |
| 压低 | 同上 | 减少 arousal |
| 锚点 | crosshair | 单点 arousal+valence |

### 2.3 行走预览（Phase 1）

| 元素 | 规格 |
|------|------|
| 小人 | 12×12 三角，沿 X 移动 |
| 播放 | 空格播放/暂停，速度 1x realtime |
| 读数面板 | 右上 `A 0.72 · V -0.41 · T 12.4s` |

### 2.4 悬崖建议 `CliffSuggestionBar`

| 属性 | 值 |
|------|-----|
| 高 | 48px，贴编辑器底 |
| 图标 | ⚠ amber |
| 按钮 | `插入黑场` `缓冲镜头` `忽略` |

### 2.5 组件树

```
AffectTerrainEditor
├── TerrainCanvas (canvas 2d)
├── TerrainToolbar (brush / anchor / duration)
├── CliffSuggestionBar
└── AffectReadout (行走数值)
```

---

## 3. 状态机

```
idle --pointer down--> drawing --up--> dirty --debounce 400ms--> saved
saved --cliff detected--> suggestion_visible
suggestion --accept black--> compiling --> idle
```

**duration 变更**：重采样 series（线性插值到新长度）。

---

## 4. 数据模型

```typescript
interface AffectEnvelope {
  projectId: string
  durationSec: number
  sampleRate: 2
  arousalSeries: number[]
  valenceSeries: number[]
  anchors: AffectAnchor[]
  updatedAt: string
}

interface AffectAnchor {
  id: string
  timeSec: number
  arousal: number
  valence: number
  label?: string
}

interface TerrainSuggestion {
  id: string
  projectId: string
  timeSec: number
  delta: number
  kind: 'cliff' | 'plateau'
  status: 'pending' | 'accepted' | 'ignored'
}
```

```sql
CREATE TABLE affect_envelope (
  project_id TEXT PRIMARY KEY,
  duration_sec REAL NOT NULL,
  sample_rate INTEGER NOT NULL DEFAULT 2,
  arousal_series TEXT NOT NULL,
  valence_series TEXT NOT NULL,
  anchors TEXT NOT NULL DEFAULT '[]',
  updated_at TEXT NOT NULL
);

CREATE TABLE terrain_suggestions (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL,
  time_sec REAL NOT NULL,
  delta REAL NOT NULL,
  kind TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'pending',
  created_at TEXT NOT NULL
);
```

### 悬崖算法（单测必备）

```typescript
export function detectCliffs(
  arousal: number[],
  sampleRate: number,
  opts = { threshold: 0.35, minDt: 0.5 }
) {
  const out: { timeSec: number; delta: number; slope: number }[] = []
  for (let i = 1; i < arousal.length; i++) {
    const dt = 1 / sampleRate
    if (dt < opts.minDt) continue
    const slope = (arousal[i] - arousal[i - 1]) / dt
    if (Math.abs(slope) > opts.threshold) {
      out.push({ timeSec: i / sampleRate, delta: arousal[i] - arousal[i - 1], slope })
    }
  }
  return out
}
```

### prompt 注入

```typescript
export function formatAffectHint(t: number, env: AffectEnvelope): string {
  const idx = Math.min(env.arousalSeries.length - 1, Math.floor(t * env.sampleRate))
  const a = env.arousalSeries[idx]
  const v = env.valenceSeries[idx]
  return `emotional tone: arousal ${a.toFixed(2)}, valence ${v.toFixed(2)}`
}
```

---

## 5. 实现路线图

### Day 1

- [ ] `affect-envelope-service.ts` CRUD
- [ ] `cliff-detector.ts` + tests
- [ ] IPC

### Day 2

- [ ] Canvas 绘制 + 画笔采样
- [ ] valence：Shift+画笔修改当前段 valence

### Day 3

- [ ] CliffSuggestionBar + 忽略/接受
- [ ] `compile-affect.ts`（探针12）黑场 → compose gap
- [ ] 注入 Video/Image generate

### Day 4–6

- [ ] 行走预览、锚点、storyboard `affectHint` 字段
- [ ] 画笔停顿 2s → `fluidBus` probe trigger

---

## 6. 代码挂接清单

```
src/components/affect/AffectTerrainEditor.tsx
src/components/affect/TerrainCanvas.tsx
src/components/affect/CliffSuggestionBar.tsx
electron/utility/services/affect/cliff-detector.ts
electron/utility/services/affect/affect-at-time.ts
electron/main/services/affect-envelope-service.ts
electron/main/ipc/affect.ts
```

**Storyboard 扩展**（`src/types/storyboard.ts`）：

```typescript
export interface StoryboardCell {
  // existing...
  startTimeSec?: number
  durationSec?: number
  affectHint?: string
}
```

---

## 7. 测试用例与验收

| ID | 用例 | 通过标准 |
|----|------|----------|
| T04-1 | 30s @2Hz series length | 60 |
| T04-2 | 阶跃 0→1 in 0.5s | detectCliffs 非空 |
| T04-3 | formatAffectHint | 含 arousal/valence |
| M04-1 | 1min 画 30s 坡 | 可完成 |
| M04-2 | 人工崖 | 3s 内建议条 |
| M04-3 | 生成日志 | 含 emotional tone |
| E3 | 对照 | 步骤数 -2 |

**降级**：仅数字输入 arousal/valence 表单，隐藏画布。

---

## 8. 风险

| 风险 | 对策 |
|------|------|
| Canvas DPI 模糊 | `devicePixelRatio` 缩放 |
| 时长与分镜不一致 | compile 时以地形为准重算 cell duration |
