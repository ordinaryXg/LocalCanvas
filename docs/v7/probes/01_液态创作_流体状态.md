# 探针 01 — 液态创作：流体状态

> **概念锚点**：温度/粘度/张力表达项目成熟度；导出是结晶非终点。  
> **依赖**：探针 12（可选，Phase 0 可独立）；探针 03/10 提供统计输入。

---

## 0. 落地原型边界

### Phase 0 MVP（2 天）

- `fluid_state` 表 + 雾池占位 UI + 三推子 + 自动温度公式
- 事件流侧栏（读 `fluid_events`）
- **不做**：WebGL 流体仿真、真实物理

### Phase 1（+2 天）

- 掬取圆环跳转 shotSlot、与探针 11 相位联动
- 关闭项目时写 `last_session_ended_at`，余温文案

---

## 1. 用户旅程

| 步 | 用户动作 | 系统响应 |
|----|----------|----------|
| 1 | 打开项目 | `fluid:getState`；无则 `temperature=0.55, phase=explore` |
| 2 | 见雾池 + 推子 | 雾色 `hsl(25, 60%, 50%)` ← temperature 映射 |
| 3 | 生成 3 个鬼影候选 | 温度 +0.15（事件 `superposed_growth`） |
| 4 | 拖动粘度到 0.8 | 尝试删结晶镜头 → ConfirmDialog |
| 5 | 导出 MP4（探针10） | 温度 -0.2，文案「已拍照·流体仍在」 |

---

## 2. 界面设计规范

### 2.1 雾池 `FluidFogView`

| 属性 | 值 |
|------|-----|
| 尺寸 | 主内容区 100%，`pointer-events: auto` |
| 背景 | 两层：`noise.png` 8% opacity 动画 + 径向渐变 |
| 色温 | `temperature` 0→1 映射 `hue 220→35`，`saturation 20%→55%` |
| 余温 | `lastSessionEndedAt` <24h：中心 `radial-gradient` 额外 +15% 亮度 |
| 长按 | 600ms 触发 `scale(0.98)` + 圆环菜单 |

### 2.2 掬取圆环 `FluidScoopMenu`

| 属性 | 值 |
|------|-----|
| 直径 | 280px，最多 3 枚缩略图 orbit |
| 缩略图 | 64px 圆，来自最近 `superposed` 非 primary |
| 点击 | `fluidBus.dispatch` → 聚焦对应 VideoGenerator |

### 2.3 推子 `FluidSliders`

| 属性 | 值 |
|------|-----|
| 宽 | 左侧 dock 72px × 3 |
| 轨道高 | 160px，拇指 24×12px |
| 标签 | 温度 / 粘度 / 张力（i18n key `fluid.temp` 等） |
| 自动标记 | 温度推子旁小点：蓝=自动，橙=手调覆盖 |

### 2.4 事件侧栏

| 属性 | 值 |
|------|-----|
| 宽 | 240px，最多 20 条 |
| 行高 | 32px，`时间 · 事件摘要` |
| 示例 | `14:02 共鸣源 +1`、`14:05 镜头进入叠加态` |

### 2.5 组件树

```
FluidFogView
├── FogLayer (div + CSS)
├── FluidScoopMenu (conditional)
├── PhaseSwitcher (探针11)
└── FluidSliders (left dock)
FluidEventFeed (right dock)
```

---

## 3. 状态机

### 3.1 温度计算（每 30s + 事件触发）

```typescript
function computeTemperature(input: {
  openSuperposedCount: number
  recentEditCount24h: number
  crystallizedRatio: number
  hoursSinceLastSession: number
}): number {
  const heat =
    input.openSuperposedCount * 0.12 +
    input.recentEditCount24h * 0.04 +
    Math.max(0, 1 - input.hoursSinceLastSession / 24) * 0.1
  const cool = input.crystallizedRatio * 0.55
  return clamp(0.15, 0.95, 0.35 + heat - cool)
}
```

手调覆盖：`userTemperatureOverride: number | null`；非 null 时 UI 显示橙点，30min 后恢复自动。

### 3.2 phase 推导（与探针 11 共享）

| 条件 | phase |
|------|-------|
| `primaryCount === 0` 或 `temperature > 0.6` | explore |
| `superposedCount > 0` 且 `0.3 < temperature ≤ 0.6` | converge |
| `crystallizedRatio > 0.5` 或手动冻结 | freeze |

### 3.3 异常态

| 场景 | UI |
|------|-----|
| IPC 失败 | 雾池仍显示，推子 disabled + toast |
| 无项目 | 不挂载 FluidFogView |

---

## 4. 数据模型

```typescript
interface FluidState {
  projectId: string
  temperature: number
  viscosity: number
  surfaceTension: number
  phase: 'explore' | 'converge' | 'freeze'
  userTemperatureOverride: number | null
  lastSessionEndedAt: string | null
  crystallizedShotIds: string[]
  updatedAt: string
}

interface FluidEvent {
  id: string
  projectId: string
  eventName: string
  payload?: Record<string, unknown>
  createdAt: string
}
```

```sql
CREATE TABLE fluid_state (
  project_id TEXT PRIMARY KEY,
  temperature REAL NOT NULL DEFAULT 0.55,
  viscosity REAL NOT NULL DEFAULT 0.35,
  surface_tension REAL NOT NULL DEFAULT 0.5,
  phase TEXT NOT NULL DEFAULT 'explore',
  user_temperature_override REAL,
  last_session_ended_at TEXT,
  crystallized_shot_ids TEXT NOT NULL DEFAULT '[]',
  updated_at TEXT NOT NULL
);

CREATE TABLE fluid_events (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL,
  event_name TEXT NOT NULL,
  payload TEXT,
  created_at TEXT NOT NULL
);
CREATE INDEX idx_fluid_events_project ON fluid_events(project_id, created_at DESC);
```

**surfaceTension 消费**：探针 02 调谐盘轨道最小间距 `minOrbit = 80 + tension * 120` px。

---

## 5. 实现路线图

### Day 1

- [ ] `migrateV7FluidSchema`：`fluid_state`, `fluid_events`
- [ ] `fluid-state-service.ts`：CRUD + `appendEvent`
- [ ] `fluid.ts` IPC + preload
- [ ] `fluidStore.ts`

### Day 2

- [ ] `FluidFogView` + `FluidSliders`
- [ ] `useFluidTemperature` 定时重算
- [ ] 项目 `load`/`unload` 钩子写 `lastSessionEndedAt`
- [ ] `FluidEventFeed`

### Day 3–4（Phase 1）

- [ ] `FluidScoopMenu` + superposed 查询
- [ ] 粘度锁与探针 10 结晶联动

---

## 6. 代码挂接清单

| 文件 | 动作 |
|------|------|
| `electron/main/services/fluid-state-service.ts` | 新建 |
| `electron/main/ipc/fluid.ts` | 新建 |
| `src/stores/fluidStore.ts` | 新建 |
| `src/components/fluid/FluidFogView.tsx` | 新建 |
| `src/components/fluid/FluidSliders.tsx` | 新建 |
| `src/components/fluid/FluidEventFeed.tsx` | 新建 |
| `src/stores/projectStore.ts` | `closeProject` 时 `fluid:endSession` |
| `src/i18n/zh-CN.json` | 新增 `fluid.*` 键 |

### IPC 列表

```
fluid:getState(projectId)
fluid:patchState(projectId, partial)
fluid:appendEvent(projectId, eventName, payload?)
fluid:listEvents(projectId, limit?)
fluid:endSession(projectId)
```

---

## 7. 测试用例与验收

| ID | 类型 | 用例 | 通过标准 |
|----|------|------|----------|
| T01-1 | 单测 | `computeTemperature` 零输入 | ∈ [0.15, 0.95] |
| T01-2 | 单测 | 4 鬼影 + 0 结晶 | temperature > 0.5 |
| M01-1 | 手动 | 打开项目 | 500ms 内雾池可见 |
| M01-2 | 手动 | 拖粘度删结晶 | 弹出确认 |
| M01-3 | 众测 | 5 人读温度 | ≥3 人判断正确 |
| M01-4 | 回归 | `FLUID_UI=0` | 无 fluid 表读写错误 |

**降级**：设置 → 创作 → 关闭液态首屏；推子收入「高级」折叠面板。

---

## 8. 风险

| 风险 | 对策 |
|------|------|
| 隐喻难懂 | 首次 15s coach mark（探针 11 引导共用） |
| 温度抖动 | 重算结果 EMA 平滑 `0.7*old + 0.3*new` |
